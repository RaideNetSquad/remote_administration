#include "network.h"
#include "ui_network.h"


/*void Network::connections()
{
    QObject::connect(connectToServ, &QTcpSocket::connected,
            this, &Network::slot_NewConnected);
    QObject::connect(connectToServ, &QTcpSocket::readyRead,
            this, &Network::slot_read);
    QObject::connect(this, &Network::closeUi,
                     this, &Network::slotCloseUi);

}*/
